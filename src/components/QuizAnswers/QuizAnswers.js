import React from 'react';

const QuizAnswers = ({answer}) => {
    return (
        <div>

            <button>{answer}</button>
            
            
        </div>
    );
};

export default QuizAnswers;